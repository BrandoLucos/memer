<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %>
</head>

<body class="container-fluid">
  <header>
    <% include ../partials/header %>
  </header>

  <main>

  <!-- all of the buttons and input fields  -->

    <div div class="jumbotron" id="edit">
      <div style="padding: 10px; font-size: 35px"; >Customize Your Meme</div>

      <form class="form-horizontal" method="POST" action="/memes/create">
        <div class="form-group">
          <label class="col-sm-2 control-label">Title</label>
          <div class="col-sm-10">
            <input type="text" name="title" value="<%= meme.title %>">
          </div>
        </div>

        <div class="form-group">
          <label class="col-sm-2 control-label">Tag</label>
          <div class="col-sm-10">
            <input type="text" name="tag" value="<%= meme.tags[0] %>">
          </div>
        </div>

         <div class="form-group hidden">
          <label class="col-sm-2 control-label">Image</label>
          <div class="col-sm-10">
            <input type="text" name="image" value="<%= meme.image %>">
          </div>
        </div>

        <div class="form-group">
          <label class="col-sm-2 control-label">Top</label>
          <div class="col-sm-10">
            <input type="text" name="top" value="<%= meme.top %>">
          </div>
        </div>

        <div class="form-group">
          <label class="col-sm-2 control-label">Bottom</label>
          <div class="col-sm-10">
            <input type="text" name="bottom" value="<%= meme.bottom %>">
          </div>
        </div>

        <div id="submit">
          <input type="submit" value="Submit">
        </div>

        <div id="imgedit">
        <img src= "<%= meme.image %>">
      </div>
      </form>

<!-- meme image to be updated -->


    </div>
  </main>

  <footer>
    <% include ../partials/footer %>
  </footer>
</body>
</html>


